/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package examen_programacion;

import Clases_DAO.CandidatoDAO;
import Clases_DAO.IndividualDAO;
import entidades.Candidato;
import static entidades.Candidato_.ciudad;
import static entidades.Candidato_.idcandidato;
import static entidades.Candidato_.nombre;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;

/**
 *
 * @author Rim
 */
public class Examen_PROGRAMACION {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
/*
        HashSet<Integer, String> candidatos;
        CandidatoDAO aDAO = new CandidatoDAO();
        candidatos = aDAO.buscarCandidatos_Finalistas();
        Iterator it2;
        while (it2.hasNext()) {
            int idcandidato = (Integer) it2.next();
            String nombre = (String) candidatos.g;
            System.out.println("Candidato: " + idcandidato + "nombre es" + nombre + " ciudad es: " + ciudad + ".");
        }

    }*/

}
}
